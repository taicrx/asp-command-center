const ASP_APP={async loadAll(){try{ASP_UTILS.setStatus("Loading data...");await ASP_DATA.loadAll();ASP_CHARTS.renderAll();ASP_AWARE.render();if(ASP_STATE.antibiogram.length){document.getElementById("antibiogramStatus").innerText=`Fact_Antibiogram_Master loaded: ${ASP_STATE.antibiogram.length} rows.`;ASP_HEATMAP.init();ASP_TRENDS.init()}else document.getElementById("antibiogramStatus").innerText="Missing Fact_Antibiogram CSV URL or no rows loaded.";ASP_AI.renderAll();ASP_UTILS.setStatus("Data loaded.")}catch(e){console.error(e);ASP_UTILS.setStatus("Load failed: "+(e.message||e));}},initTabs(){document.querySelectorAll(".tab").forEach(btn=>btn.addEventListener("click",()=>{document.querySelectorAll(".tab").forEach(x=>x.classList.remove("active"));document.querySelectorAll(".page").forEach(x=>x.classList.remove("active-page"));btn.classList.add("active");document.getElementById(btn.dataset.target).classList.add("active-page")}))}};document.addEventListener("DOMContentLoaded",()=>{ASP_APP.initTabs();ASP_APP.loadAll()});